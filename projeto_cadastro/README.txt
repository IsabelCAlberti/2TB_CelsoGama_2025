# Projeto: Tela de Cadastro com Validação (Flask + HTML + CSS + JavaScript)

Este projeto é um exemplo simples de um sistema de cadastro de usuário, com validação de e-mail, senha forte e confirmação de senha.

## 🎯 Funcionalidades

- Validação de e-mail (formato válido, tipo: usuario@gmail.com)
- Validação de senha (mínimo 8 caracteres, com pelo menos:
  - 1 letra maiúscula
  - 1 número
  - 1 caractere especial)
- Verificação de confirmação de senha
- Feedback de sucesso ou erro exibido na própria página (sem recarregar)

## 🚀 Tecnologias utilizadas

- **Python 3.x**
- **Flask**
- **HTML5**
- **CSS3**
- **JavaScript (Fetch API para AJAX)**

## ✅ Requisitos para rodar o projeto

- Python 3 instalado
- Pip instalado
- Flask instalado

Para instalar o Flask, rode:

```bash
pip install flask
